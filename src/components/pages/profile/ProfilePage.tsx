import React from 'react'

// ✅ 타입 선언

const ProfilePage = () => {
    // ✅ 상태 (State) 관리

    // ✅ 훅 사용 (Alert 등)

    // ✅ 사이드 이펙트 (useEffect 등)

    // ✅ 함수

    return <div>ProfilePage</div>
}

export default ProfilePage
