import React from 'react'

// ✅ 타입 선언

const LoginPage = () => {
    // ✅ 상태 (State) 관리

    // ✅ 훅 사용 (Alert 등)

    // ✅ 사이드 이펙트 (useEffect 등)

    // ✅ 함수

    return <div>LoginPage</div>
}

export default LoginPage
